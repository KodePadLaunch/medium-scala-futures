name := "ScalaFutures"

version := "0.1"

scalaVersion := "2.13.1"

fork in run := true

// javaOptions in run += "-Dscala.concurrent.context.minThreads=4"
// javaOptions in run += "-Dscala.concurrent.context.numThreads=4"
// javaOptions in run += "-Dscala.concurrent.context.maxThreads=4"
